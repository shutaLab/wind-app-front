export interface Calendar {
  title: string;
  content: string;
  start_date: string;
  end_date: string;
  is_absent: boolean;
}

export interface CalendarId {
  id: number;
  title: string;
  content: string;
  start_date: string;
  end_date: string;
  is_absent: boolean;
}
